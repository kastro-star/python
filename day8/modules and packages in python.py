#modules---
import Recursionfunction
from scope_of_variable import add

from Recursionfunction import factorial
from scope_of_variable import *  #* is used to import all the functions from the module
import math  # Replace 'from math import *' with this

print(add())
print(factorial(5))
print(num1)
print(num2)
print(math.sqrt(16))

#create __init__.py file to create a package
